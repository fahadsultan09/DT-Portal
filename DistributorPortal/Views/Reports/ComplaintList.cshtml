@model IEnumerable<Complaint>

<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-header">
                <h2 class="card-title">Complaint List</h2>
            </div>
            <div class="card-body scrollmenu">
                <table id="example1" class="table table-striped table-hover" style="width:100%">
                    <thead>
                        <tr>
                            <th>Complaint No</th>
                            @if (!SessionHelper.LoginUser.IsDistributor)
                            {
                                <th>Distributor Name</th>
                            }
                            <th>Status</th>
                            <th>Category</th>
                            <th>Sub Category</th>
                            <th>Complaint By</th>
                            <th>Complaint Date</th>
                            <th>Approved By</th>
                            <th>Approved Date</th>
                            <th>Rejected By</th>
                            <th>Rejected Date</th>
                            <th>Action</th>
                        </tr>
                    <tbody>
                        @if (Model != null)
                        {
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>@string.Format("{0:1000000000}", item.Id)</td>
                                    @if (!SessionHelper.LoginUser.IsDistributor)
                                    {
                                        <td>@item.Distributor.DistributorName</td>
                                    }
                                    <td>@Html.DisplayFor(x => item.Status)</td>
                                    <td>@item.CreatedName</td>
                                    <td>@item.ComplaintCategory.ComplaintCategoryName</td>
                                    <td>@item.ComplaintSubCategory.ComplaintSubCategoryName</td>
                                    <td>@item.CreatedDate</td>
                                    <td>@item.ApprovedName</td>
                                    <td>@item.ApprovedDate</td>
                                    <td>@item.RejectedName</td>
                                    <td>@item.RejectedDate</td>
                                    <td>
                                        <a asp-controller="Complaint" asp-action="Add" asp-route-id="@item.Id" class="btn btn-info btn-sm">
                                            <i class="fas fa-eye"></i>
                                        </a>
                                    </td>
                                </tr>
                            }
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

<script type="text/javascript">

    $(document).ready(function () {

        var table = $('[Id$=example1]').DataTable({
            "lengthMenu": [[5, 10, 25, 50, -1], [5, 10, 25, 50, "All"]],
        });

    });

</script>
