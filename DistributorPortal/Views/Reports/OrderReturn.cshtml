@model OrderSearch
@inject IUnitOfWork iUnitOfWork
@{
    ViewBag.Title = "Order Return ";
    var distributor = new DistributorBLL(iUnitOfWork).DropDownDistributorList(null);
    var search = new OrderReturnSearch() { DistributorList = distributor };
}

<div class="content-wrapper">
    <section class="content-header">
        <div class="container-fluid">
            <div class="row mb-2">
                <div class="col-sm-6">
                    <h1>Order Retrun</h1>
                </div>
                <div class="col-sm-6">
                    <ol class="breadcrumb float-sm-right">
                        <li class="breadcrumb-item"><a href="#">Home</a></li>
                        <li class="breadcrumb-item active">Order Return</li>
                    </ol>
                </div>
            </div>
        </div>
    </section>
    <partial name="OrderReturnSearch" model="@search" />
    <section class="content">
        <div class="container-fluid scrollmenu" id="divform">
            <partial name="OrderReturnList" model="Model" />
        </div>
    </section>
</div>